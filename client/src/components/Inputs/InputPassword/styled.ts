import styled from "styled-components";

export const EyeButton = styled.button`
  position: absolute;
  transform: translateY(-50%);
  top: 70%;
  right: 16px;
  background: none;
  border: none;
  cursor: pointer;

  svg {
    width: 22px;
    height: 22px;
    color: #aaa;
  }
`;
